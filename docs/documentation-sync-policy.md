# Documentation Sync Policy

Automatic documentation sync may update only files in `docs/auto-sync/`.
Core docs such as README files, contracts, and product specs require human review.
The sync workflow can use Claude and Codex steps when the relevant repository secrets are present.
